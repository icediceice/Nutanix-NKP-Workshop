apiVersion: snapshot.storage.k8s.io/v1
kind: VolumeSnapshot
metadata:
  name: postgres-snapshot
  namespace: demo-app
spec:
  volumeSnapshotClassName: nutanix-snapshot
  source:
    persistentVolumeClaimName: data-postgres-0
