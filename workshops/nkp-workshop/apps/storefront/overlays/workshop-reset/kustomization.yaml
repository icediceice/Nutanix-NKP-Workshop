apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
# Workshop Reset: minimal resources — just namespace + demo-wall + load off.
# ArgoCD prune:true will delete everything else when switching to this overlay.
resources:
  - ../../base/namespace.yaml
  - ../../../../demo-wall/overlays/workshop-reset/
  - ../../../../loadgen/overlays/load-off/
