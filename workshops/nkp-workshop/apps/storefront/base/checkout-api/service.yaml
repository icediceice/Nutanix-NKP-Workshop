apiVersion: v1
kind: Service
metadata:
  name: checkout-api
  namespace: demo-app
  labels:
    app: checkout-api
spec:
  selector:
    app: checkout-api
  ports:
    - name: http
      port: 8080
      targetPort: 8080
