# Copy to secret.yaml, fill in your key, then: kubectl apply -f secret.yaml
# secret.yaml is gitignored — never commit your real API key.
apiVersion: v1
kind: Secret
metadata:
  name: anthropic-key
  namespace: demo-triage
type: Opaque
stringData:
  api-key: "sk-ant-REPLACE_ME"
