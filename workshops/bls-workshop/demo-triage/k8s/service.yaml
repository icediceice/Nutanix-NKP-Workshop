apiVersion: v1
kind: Service
metadata:
  name: triage-app
  namespace: demo-triage
spec:
  type: NodePort
  selector:
    app: triage-app
  ports:
  - port: 80
    targetPort: 8000
    nodePort: 30090
